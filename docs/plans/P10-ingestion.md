# P10 — Document ingestion, on AWS

Phase B of [AWS-NATIVE-BRIEF.md](AWS-NATIVE-BRIEF.md). The phase that makes the pipeline
AWS-native: S3 upload, Step Functions fan-out over chunks, Bedrock generation, DynamoDB
job state — and it ends the SSE stream that P2 built.

**Reference:** the brief's D3, D5, D6, D10, §6 cost controls 1–3, §7 open questions 1, 2,
3, 4, 6, 7, §8 constraints 1, 3, 8, 10. P9's [ADR 0008](../adr/0008-application-level-tenancy.md)
governs every new table and every new data-access module; it is not reopened here.

**Done when:** a user drops a PDF on `/create/document`, watches honest progress, and ends
at the review gate with cards generated by Bedrock — and `/create/text` still works,
having moved to the same pipeline rather than keeping a second one.

**Starting fresh?** Read [P10-SESSION-1.md](P10-SESSION-1.md) first — it is the handover
from P9 and covers how to bring the local backend up in two commands.

---

## What P9 leaves you, stated plainly

Read this before the tasks; several of them exist only because of it.

**P9 is finished and, unusually for this project, largely *proven*.** Nine of its ten
acceptance criteria are met. The code was not merely typechecked — it was executed, and
the execution found three real bugs that are already fixed. See
[P9-aws-slice.md](P9-aws-slice.md) for the recorded evidence.

### The hybrid you are inheriting, and why it exists

**RDS is the only billable line in P9** (~$14/mo, verified against the AWS Pricing API),
and the owner's AWS credits are the budget for *this* phase's Bedrock work. So P9 was
finished without deploying it:

| Piece | Where it runs today | Cost |
| ----- | ------------------- | ---- |
| Cognito | **Deployed**, `us-east-1_8byyB8D2H` | $0 — free to 50k MAU |
| Foundation (budgets, alarms) | **Deployed** (P8) | $0 |
| Postgres | **Local**, PG 18 on `localhost:5432` | $0 |
| The API | **Local**, `scripts/dev-api.mjs` | $0 |
| `SynapseDeck-Data-dev` (RDS + VPC) | **Written, synthesising, not deployed** | — |
| `SynapseDeck-Api-dev` | **Written, synthesising, not deployed** | — |

**`SynapseDeck-Api-dev` cannot be deployed on its own.** Its Lambdas join the VPC and take
the database by reference, so `cdk deploy` on it creates `AWS::RDS::DBInstance` as well —
`cdk diff` shows this plainly. Deploying "just the API" spends the AI budget on an idle
database. That is the trap this phase must not walk into.

`scripts/dev-api.mjs` stands in: it runs **the real handlers, unmodified**, behind **real
Cognito token verification** against the pool's live JWKS. It is a development tool, never
deployed, and its route table is a hand-maintained mirror of `infra/lib/api-stack.ts`.

### What is now genuinely proven

| Thing | Evidence |
| ----- | -------- |
| The schema | Both migrations applied clean to PG 18; ledger recorded |
| `review_card` / `undo_last_review` | Called for real. Cross-tenant → `PT404`, stale → `PT409`, append-only trigger → `PT403` |
| The data layer | Every function executed. All cross-tenant probes → `[]` / `null` |
| The handlers | 32 checks passing, every cross-tenant call → **404, never 403** |
| The SQLSTATE contract | The four codes `http.ts` maps are the four the database raises |
| Cognito sign-up and SRP sign-in | A real account, a real token, `profile.id === token.sub` |
| Token verification | Forged `sub`, id-token-as-access-token, stripped signature → all 401 |

### What is still unproven, and it is a short list

1. **API Gateway itself** — its route matching and its JWT authorizer never ran. `sub` was
   verified by `dev-api.mjs` instead. So *"the boundary holds given a correct `sub`"* is
   evidence; *"only API Gateway's authorizer can supply that `sub`"* is not.
2. **RDS in place of local Postgres.** PG 18 locally vs **PG 17** pinned in
   `infra/lib/data-stack.ts` — nothing in this schema differs across that line, but it has
   not been run on 17.
3. **Cold start for a Lambda in a VPC.** P9 criterion 9, still open. It cannot be measured
   without a deployed Lambda, and it is the number that decides whether the interactive
   paths need provisioned concurrency.
4. **The client against the API.** `queries.ts` was rewritten to call `api-client.ts` and
   the browser has never made one of those calls. The row types agree with the server's by
   transcription.

All four close together, at the moment RDS is deployed.

**And one thing P9 could not do at all:** `npm run demo:seed` is broken by the split — its
value is driving the real generation pipeline, which writes into Supabase, while decks and
cards moved to RDS. **P10 is the phase that fixes it**, because P10 is where generation
moves. See task 11.

---

## Preconditions

```bash
git branch --show-current      # aws-native
git status                     # clean
npm run verify                 # green
npm run infra:synth            # all eight stacks synthesise

# The local backend P9 left behind. Both must come up before any task here.
pg_isready                     # PG 18 on localhost:5432
npm run dev:api                # the real handlers, on :8787
```

- **P9 is complete and largely proven.** Everything above.
- **The local database must exist.** Postgres 18, database `synapsedeck`, role
  `synapsedeck_app`. If it is gone, recreate it and run `npm run db:migrate` — the
  migrations are idempotent through the ledger and take seconds.
- **`.env.local` holds the Cognito ids and points `VITE_API_URL` at `http://localhost:8787`.**
  It is gitignored, so a fresh clone needs it rebuilt from `.env.example` plus the Auth
  stack's outputs.
- **⚠ Bedrock model access is BLOCKED, checked 2026-09-06.** Two independent failures,
  both found by invoking rather than by reading status fields — which matters, because
  `get-foundation-model-availability` reports Haiku 4.5 as `AUTHORIZED` with entitlement
  `AVAILABLE` and it still cannot be called:
  1. **Anthropic models refuse the caller’s country.** `ValidationException: Access to
     Anthropic models is not allowed from unsupported countries…` — the requests originate
     from the UAE. This is about where the call comes from, so no IAM policy, model grant
     or CDK change fixes it.
  2. **The account is unverified**, which blocks every provider: Amazon Nova returns
     `AccessDeniedException: Your account is currently being verified.` This one is
     time-bounded and may have cleared — re-test it.

  **The owner has decided Bedrock stays in `us-east-1`**, so the region is not a variable
  to trade away. See [P10-SESSION-2.md](P10-SESSION-2.md) for the commands and what is
  still buildable meanwhile — tasks 2, 3, 4, 5, 6 and 8 need no live model, and D6’s
  provider interface exists so `GROQ_API_KEY` can stand in. **Do not write Bedrock code
  that assumes a successful call and leave it untested.**
- **The credits are this phase's budget, and they are finite.** $140, expiring 2027-03-03,
  $0 spent so far. The brief's §6 estimates $20–40/month for development once Bedrock is
  in play, so the runway is real but not generous. **Two consequences that are constraints,
  not advice:**
  1. **Do not deploy RDS until task 1 says to.** It is ~$14/mo whether or not anything
     queries it, and `cdk deploy SynapseDeck-Api-dev` creates it as a side effect.
  2. **The §6 cost controls are implemented, not noted** (§8 constraint 10). Every Bedrock
     call in this phase is capped and cached before it is run twice.

---

## Out of scope — do not build these here

- **Exams, questions, attempts, grading.** Phase C. The exam runner already on this branch
  (`src/features/exam/`, running on fixtures) stays on fixtures — see task 13.
- **Topics as a *feature*.** Task 7 settles where topics come from because D11 depends on
  it and Phase C cannot start without it, but the mastery map stays on fixtures.
- **pgvector and semantic search.** Phase G.
- **Retiring Supabase.** Phase F. `/progress` still reads it after this phase.
- **Deploying `prod`.** Still owner-only, still deferred to a checkpoint. Dev only.
- **Reintroducing a test suite.** ADR 0005 stands until the owner says otherwise. If the
  first-run debugging in task 1 makes the case for one, **say so and stop** — that is a
  decision.

---

## The open questions this phase must settle

The brief's §7 lists these as genuinely open. A plan that ends without answering them has
not finished, because Phase C inherits the ambiguity.

| # | Question | Settled by |
| - | -------- | ---------- |
| 1 | Quota shape for multi-chunk work | Task 8 |
| 2 | Progress reporting once it is not an SSE stream | Task 5 |
| 3 | How a half-failed job presents to the user | Task 6 |
| 4 | The draft-card seam — DynamoDB drafts, Postgres acceptance | Task 4 |
| 6 | Scanned PDFs have no text layer | Task 3 |
| 7 | Where topics come from and how stable they are | Task 7 |

§7 question 5 — the PGlite harness — is **already answered and needs no task**: the
harness was deleted with the suite (ADR 0005), so there is nothing to re-point.
`supabase/pg-version.json` still describes the Supabase project, which is correct until
Phase F retires it; the RDS major is pinned in `infra/lib/data-stack.ts`.

---

## Tasks

Ordered so the app runs after every one.

### 1. Start local. Deploy nothing.

**The session that opens this phase does not touch AWS.** P9's first-execution debugging
is done — the thing this task used to be — so what is left is to confirm the inherited
backend still runs and then get on with the pipeline.

```bash
pg_isready && npm run db:migrate:status   # both migrations applied
npm run dev:api                            # :8787, real handlers, real token checks
npm run dev                                # the SPA, pointed at :8787
```

**Then do the one thing P9 could not**: sign in through the browser and drive the app.
P9 proved the handlers over HTTP with a script; it never proved `queries.ts` against
`api-client.ts` from a real browser, and the client's row types agree with the server's by
transcription alone. Expect to find mismatches — a field the client reads that the API does
not send is exactly the shape of bug left in that gap, and it is cheap to find here.

Fix what breaks. That is the whole of task 1.

**✅ Done 2026-09-06.** Driven against `dev-api.mjs` with a real Cognito access token,
from a throwaway pool user created and deleted for the run. All 18 routes exercised:
profile read and update, deck create/read/update/delete, cards create/list/edit/status/
delete/accept, the queue, the summary, a rating, an undo, and the finish-gate.

**The predicted bug was not there, and a subtler one was.** The row-shape mismatch this
task expected does not exist: every field `queries.ts` reads exists in the RDS schema with
matching nullability across `profiles`, `decks` and `cards`, all five enums match
member-for-member, and `COLUMNS = '*'` in the data layer means the server cannot omit a
field the client wants. Compared mechanically, not by eye.

What running it found instead was **the timestamp format** (fixed, `c854e8e`). Every
timestamp left the API as Postgres's own `2026-09-06 03:43:16.065206+04` — a space where
ISO 8601 has a T — and `fsrs.ts` parses `due` and `last_review` with `new Date()` to
compute intervals. That format is not one the ECMAScript specification requires any engine
to parse; V8 accepts it, which is why Node and Chrome were fine and nothing caught it.
Safari has historically rejected it, where it would be an `Invalid Date` feeding FSRS —
presenting as a broken scheduler rather than as a parse error.

The fix had a trap in it worth recording, because the obvious version is worse than the
bug. `updated_at` is the optimistic-concurrency token and `review_card` compares it as
`$5::timestamptz`; Postgres stores **microseconds** and a JS `Date` holds milliseconds,
so parsing and re-serialising truncates `.065206` to `.065` and every rating would start
failing `PT409`. The conversion is therefore a pure string transform on the wire text,
carrying the fraction across untouched. Confirmed by a rating with a millisecond-truncated
token still being refused with `PT409`.

Two other things worth having on the record, both stronger than what P9 could claim:

- **The cross-tenant probes were re-run as a real Cognito identity**, not P9’s synthetic
  `1111…`/`2222…` users. `GET`, `PATCH`, `DELETE` and `POST …/cards` against another
  user's deck all return **404, never 403**; listing its cards returns `[]`.
- **Optimistic concurrency was exercised for real**: a stale token and a truncated token
  are both refused with `PT409`, and a correct one graduates the card.

**✅ Closed 2026-09-06 (session 2).** The owner drove the app in a browser against
`dev-api.mjs` on `localhost:5173` and reported criterion 7 passing. React Query's cache
keys, the optimistic updates in `useReviewCard`, and the forms all behave against the RDS
handlers. **Criterion 7 is green**, and with it the last place a P9-era bug could hide on
the client side.

What that leaves unproven is only what was always deferred to task 12: API Gateway's own
routing and authorizer, PG 17 rather than 18, and the Lambda-in-VPC cold start. Those need
a deploy, not a browser.

**Deferred to the RDS checkpoint** (see task 12), and deliberately not done now:

- Deploying `SynapseDeck-Data-dev` and `SynapseDeck-Api-dev`
- P9 criterion 9, the Lambda-in-VPC cold start
- API Gateway's own routing and authorizer
- Running the migrations against PG 17 rather than local PG 18

### 1b. Keep `dev-api.mjs` honest

`scripts/dev-api.mjs` mirrors `infra/lib/api-stack.ts` by hand: a route added to one and
forgotten in the other is a bug that only appears at the RDS checkpoint, weeks later, in
the least convenient place.

**Every task below that adds a route adds it in both files, in the same commit.**

**✅ Done 2026-09-06** (`cb9cb4f`), and mechanical rather than a convention.
`scripts/check-routes.mjs` parses both route tables and compares them as sets of
`METHOD /path`, with path parameters normalised so a rename is not drift. It runs in
`verify`. Currently 18 routes, identical.

It reports which direction drifted, because they are not equally dangerous: a route only
in `api-stack.ts` 404s locally and is found at once, while one only in `dev-api.mjs`
works all through development and fails only once deployed. It also fails if it ever
parses zero routes from either file — a check that silently stops matching would pass
forever and guard nothing.

Verified by injecting drift in both directions and confirming each was caught and
correctly attributed. Closes acceptance criterion 10.

### 2. Job state in DynamoDB — `infra/lib/pipeline-stack.ts`

D3's other half. One table, single-table design, on-demand billing (free at idle, and this
is idle almost always).

- Partition key `userId`, sort key `jobId#…`. **`userId` is the partition key deliberately**
  — it makes the tenancy boundary a property of the key rather than a filter, which is a
  genuinely stronger position than the SQL side has (ADR 0008 admits as much). Do not model
  it any other way for convenience.
- TTL on job records. Pipeline state is not history: a finished job is interesting for
  hours, not months, and a TTL is what stops this table becoming a second database nobody
  meant to keep.
- **A data-access module, under the same four rules.** `services/api/src/data/jobs.ts`,
  `userId` first, required. `scripts/check-data-access.mjs` enforces rules 1 and 3 whether
  the store is Postgres or DynamoDB — its checks are about the shape of the function, and a
  DynamoDB call in a handler is exactly as wrong as SQL in one. **Extend the lint's
  patterns to catch `DynamoDBClient` / `send(` in a handler** rather than leaving a gap it
  was never taught about.
- **DynamoDB needs a VPC endpoint** — a *gateway* endpoint, which is free, exactly like
  S3's. Add it to the Data stack. Without it the SDK call from a VPC Lambda hangs until it
  times out, which presents as a mysteriously slow function rather than a connection error
  (`infra/lib/data-stack.ts` already warns about this trap).

**✅ Done 2026-09-06** (`4457c03`). `SynapseDeck-Pipeline-{dev,prod}`, wired into
`bin/app.ts` before the Api stack so the table can be passed by object reference.

Built as specified: `userId` partition key, compound `sk` (`job#<id>` and
`job#<id>#chunk#<n>`), on-demand billing, TTL on `expiresAt`, and the free DynamoDB
gateway endpoint added to the Data stack beside S3's.

Three things decided while building, worth having on the record:

- **Chunk indexes are zero-padded** (`chunk#000007`). DynamoDB sorts sort keys
  lexicographically, so unpadded `chunk#10` sorts before `chunk#2` and a `Query` returns
  the chunks shuffled. Cheap to get right now, confusing to diagnose later.
- **The TTL is computed in exactly one place**, because the milliseconds version of that
  bug is silent rather than loud: a millisecond timestamp is a date ~50,000 years out, so
  items simply never expire and the table grows exactly as it would with no TTL at all.
  There is no error and no symptom until a bill.
- **`grantReadWriteData` went to the cards handler alone**, not to all four. Every handler
  gets `JOB_TABLE_NAME` through `commonEnvironment`, but only the one that owns the review
  gate can touch job state. Confirmed against the synthesised template: exactly one IAM
  policy carries `dynamodb:*` and it is attached to `CardsFn`'s role only.

**The lint was extended, and the extension was tested in both directions.**
`scripts/check-data-access.mjs` matched SQL shapes only, so a `DynamoDBDocumentClient`
call in a handler passed every gate in the repository. It now also catches the client
constructors, the command classes, and a client-shaped `.send(`.

The `.send(` pattern deliberately requires a receiver (`client`, `ddb`, `docClient`, …)
rather than matching bare `send(`: `res.send()` and `notifier.send()` are ordinary code,
and a lint that fires on them is one that gets disabled instead of obeyed. Verified with
throwaway probe handlers — a direct client and an indirectly obtained one are both
caught, while `res.send()` and `notifier.send()` still pass — then the probes were
deleted. The rule id is now `no-datastore-in-handlers`.

**What is not proven: the data layer has never executed.** `services/api/src/data/jobs.ts`
typechecks and lints, and nothing has run a single one of its functions against a real
table — nothing is deployed, and there is no local DynamoDB. Every claim about the
condition expressions, the atomic `add` on `chunksCompleted`, and the `begins_with` query
is a claim about code that has been read, not run. Task 5 is where they first execute.

*(Environment note: `cdk synth` into the default `infra/cdk.out` fails on this machine with
an `EPERM` renaming a bundling temp directory. It is pre-existing and environmental —
identical on a clean tree at `a1a9b59` — not a fault in any stack. `--output` to a
different directory synthesises everything fine, and that is how the IAM grant above was
checked.)*

### 3. Upload — S3, presigned, and the scanned-PDF check

§7 question 6, and the browser half is worth more than any server-side cleverness.

- Presigned PUT from a small Lambda; the browser uploads directly to S3. The file never
  passes through the API, which is what keeps a 20 MB PDF off a Lambda's memory.
- Object key prefixed by `userId` (D4: *"`sub` becomes `userId` everywhere — S3 object
  prefixes"*), so the bucket policy can scope access by prefix and the boundary is visible
  in the key.
- **Detect a missing text layer in the browser, before the upload.** Parse the first page
  or two client-side; if there is no extractable text, say so *then* — "this looks like a
  scan; text extraction will not work on it" — rather than after a 20 MB upload and a
  failed pipeline. That is the answer to §7 question 6 and it is a product decision as much
  as a technical one.
- No OCR. Say so in the copy. Textract is a real answer and it is out of scope for this
  phase; do not add it because it is tempting.
- Lifecycle rule: delete uploads after N days. The source document is an input, not an
  archive, and S3 is only free to 5 GB.

**◐ Mostly done 2026-09-06** (session 2). Everything except the scanned-PDF check,
which the owner chose to defer rather than take the PDF-parser dependency in this pass.

Built:

- **`SynapseDeck-Pipeline-*` grew an upload bucket.** SSE-S3, all public access blocked,
  SSL enforced, CORS scoped to the SPA's origin. `corsOrigin` moved up in `bin/app.ts` so
  the bucket and the API take the same value — a mismatch there fails the browser's
  preflight and reads as a broken upload rather than a misconfigured bucket.
- **Lifecycle: 3 days**, plus `abortIncompleteMultipartUploadAfter: 1 day`. The second one
  matters more than it looks: an abandoned multipart upload leaves parts that are billed
  as storage but do not appear in the object listing, which is the classic way an
  apparently empty bucket grows a bill.
- **`POST /uploads`**, in both route tables (19 routes, parity green). It returns a
  presigned PUT and nothing else.
- **`data/uploads.ts`**, in `data/` deliberately. It is not a datastore module, but the
  object key *is* the tenancy boundary, so it belongs in the directory ADR 0008 makes
  auditable. `userId` is the first parameter and the only source of the prefix.
- **`/create/document`**, with drag-and-drop, real upload progress, and copy that says
  what it cannot do.

Four decisions worth recording:

1. **The key is generated, never taken from the client.** `uploads/<userId>/<uuid>.pdf`.
   The user's filename is display metadata only — a key built from a filename is a
   path-traversal bug waiting to be written, and it also invites collisions between users.
2. **The size limit is signed into the URL** (`ContentLength`), not merely checked in the
   handler. A handler check validates a *claim the client made*; signing it means S3 itself
   refuses a body of a different size, so a caller who lies or reuses a URL is rejected by
   S3 rather than trusted by us.
3. **`grantPut`, not `grantReadWrite`.** A presigned URL can only carry permissions the
   signing role holds, so a wider grant here would widen every URL the handler ever issues.
   Confirmed against the synthesised template: the uploads role has `s3:PutObject` and
   friends, no `GetObject` and no `DeleteObject`, and it is the only role with any S3
   permission at all.
4. **The browser's PUT does not go through `api-client.ts`.** That module attaches
   `Authorization: Bearer` to everything, which is right for the API and wrong for S3 — the
   presigned URL carries its own signature, and an unexpected `Authorization` header makes
   S3 reject the request with an error that reads as though the URL were bad.
   `XMLHttpRequest` rather than `fetch`, because `fetch` still cannot report upload
   progress and a 20 MB upload with no progress bar is indistinguishable from a hang.

**Deferred, and it is the part §7 question 6 actually asks about: the scanned-PDF check.**
The page tells the user up front that the text must be selectable and that scans will not
work, which is the copy half of the answer. Detecting a missing text layer *before* the
upload needs a PDF parser on the client — `pdfjs-dist` is ~350 KB gzipped, which would
roughly double the largest existing chunk unless it is dynamic-imported on this route
alone. That is a dependency decision the owner deferred rather than a task that was
forgotten. **Acceptance criterion 2 is therefore not met yet.**

**What is not proven.** Nothing here has run against real S3: nothing is deployed, so no
presigned URL has ever been signed or used, and the CORS rule has never been exercised by
a browser. `POST /uploads` was confirmed *registered* — it returns 401 without a token
rather than 404 — and that is the whole of what has been executed. `data/uploads.ts` has
never signed anything.

### 4. The draft-card seam — §7 question 4

**Settle this before writing the state machine**, because it decides what every chunk task
returns.

The brief says drafts live in DynamoDB and acceptance writes to Postgres. That is one
audited write path, and the whole question is where the boundary sits:

- Chunks write draft cards into DynamoDB, keyed by job. Nothing touches Postgres during
  generation, so a failed job leaves no half-written deck.
- **Acceptance is one API call** that reads the job's drafts, validates every payload with
  the shared `CardPayload` schema, and inserts into `cards` in one transaction. One path,
  in `services/api/src/data/`, under the four rules.
- The review gate reads drafts from DynamoDB, not from `cards`. **This changes
  `useDraftCards`**, which P9 pointed at `GET /decks/{id}/cards?status=draft`.

  **✅ Settled by the owner, 2026-09-06: `cards.status = 'draft'` is cut, not kept as
  legacy.** If drafts never reach Postgres before acceptance, no row can ever hold that
  value, and an enum member nothing can produce is worse than none — it invites code
  handling a state that cannot occur.

  Eight sites, both sides of the split: a new migration dropping the enum value,
  `lib/rows.ts` (`CardStatus`), `data/cards.ts` (`listDraftCards`, and `acceptDrafts`’s
  `and status = 'draft'` guard), `data/decks.ts` (`draft_count`), `handlers/cards.ts`
  (the `?status=draft` branch — so **both route tables change**, and `check:routes`
  enforces that), `queries.ts` (`useDraftCards`), and a regenerated `types/database.ts`.

  **Three traps**, spelled out in [P10-SESSION-2.md](P10-SESSION-2.md): Postgres has no
  `DROP VALUE` so this is a destructive migration that needs the owner’s go-ahead and
  fails if any row still holds `'draft'`; `types/database.ts` is generated from
  **Supabase**, which keeps the value until Phase F, so the two schemas will legitimately
  disagree; and **`decks.status = 'draft'` is a different enum and stays** — it is the
  resumable gate state. Only `card_status` loses a value.

Write the answer into `SPEC.md`, not only into code: it is a change to what a "draft" is.

### 5. Step Functions, and the end of SSE — §7 question 2

D5. Map state over chunks, per-chunk retry, partial failure as a normal outcome.

- **Job-status polling replaces the SSE stream.** The brief calls this "boring and probably
  correct"; it is also the only option that survives a Lambda that cannot hold a connection
  for the length of a fan-out. `GET /jobs/{jobId}` returns status, per-chunk progress and a
  card count.
- Poll interval that backs off. A job takes tens of seconds; polling every 500 ms for the
  whole of it is a cost and a rate limit for no extra information.
- **`src/lib/sse.ts` and `src/lib/ndjson.ts` become dead code** when `/create/text` moves
  (task 9). Delete them in that commit rather than leaving two decoders nobody calls —
  `supabase/functions/_shared/ingest.ts` re-exports them, so this is coordinated with
  retiring the Edge Function's use of them, not before.

### 6. Partial failure, in the UI — §7 question 3

"31 of 40 chunks produced cards; 9 failed" is the normal case, not the exception, and the
brief says so.

- The job record carries per-chunk status. The UI says how many chunks failed and offers
  the cards that did arrive — a fan-out that discards 31 good chunks because 9 failed is
  worse than one that admits the gap.
- Retry is per chunk, in the state machine, before the user ever sees a failure. What
  reaches the UI is what failed *after* retries.
- **Do not hide it.** A deck that quietly contains three quarters of a document is a
  product that lies. One line at the review gate.

### 7. Where topics come from — §7 question 7

**D11 depends on this and Phase C cannot start without it**, which is why it is here rather
than in Phase C.

The failure mode is specific: a user studying one subject across five uploads must not get
five overlapping topic sets, or the mastery map is meaningless.

- Extract topics per document with the model, then **reconcile against the user's existing
  topics** rather than creating fresh ones. The reconciliation is the actual work; the
  extraction is easy.
- Reconcile by name first — cheap, and catches the common case. Anything ambiguous is where
  Phase G's pgvector would help; **do not build embeddings here**, and say in the ADR that
  the phase knowingly accepted a weaker match to avoid pulling Phase G forward.
- `topics` is a new table, so: `user_id not null`, indexed, **and a data-access module with
  all four rules on the day it is created**. A new table on RDS without one is a
  cross-tenant leak, not a TODO (`CLAUDE.md`).
- **This deserves an ADR** — the brief's D11 anticipated one "if the planning session finds
  the schema contentious", and topic stability is exactly the contentious part.

### 8. Quota for multi-chunk work — §7 question 1

A *product* decision, and the brief says so. Today: 30 generations a month, one card
generation = one unit.

- Decide what a document costs. "One document = 12 units" is the brief's own illustration,
  not a decision.
- **Charge by work done, not by button press.** A 3-page PDF and a 300-page textbook are
  not the same unit, and a quota that says they are is one a user will find unfair the
  first time it matters.
- The quota must be **checkable before the job starts** — refusing at chunk 30 of 40, after
  spending the money, is the worst version of this.
- `generations` currently lives on Supabase and is read by `useQuotaUsage`. **This phase
  moves it**, because this phase is what writes it. That is the split table's own schedule
  (`generations` + quota → Phase B), so update the table in
  [P9-aws-slice.md](P9-aws-slice.md) when it moves rather than leaving it stale.

### 9. `/create/text` moves to the same pipeline

§8 constraint 8: no feature is built twice.

- The text path becomes a one-chunk job through the same state machine. Not a second code
  path with a shared schema — the *same* path.
- **The Edge Function stops being called.** Leave it deployed until Phase F (it is what
  `/progress`-era code still expects), but nothing in the app invokes it after this task.
- `GROQ_API_KEY` stays as the fallback provider behind D6's interface, now called from
  Lambda rather than from Deno.

### 10. Bedrock, behind the interface D6 describes

- One provider interface, two implementations, sharing the Zod schemas at the boundary
  that `src/lib/schemas.ts` already is. That sharing is the existing pattern
  (`services/api/src/lib/schemas.ts` re-exports for exactly this reason) — follow it.
- Haiku-class for routine generation. Larger models only behind an explicit action.
- **The three cost controls from §6 are constraints, not advice** (§8 constraint 10):
  cap the work per call, cache blueprints by document version + settings, and generate
  explanations on demand. Two of the three belong to Phase C, but **the caching seam is
  built here** or Phase C retrofits it.
- Token and cost accounting per job, into DynamoDB (D3 puts it there). This is what makes
  "$0.45 per full loop" a measured number rather than an estimate — and the eval harness
  D6 calls "the valuable artifact" needs it.

### 11. Fix `demo:seed`

P9 broke it and could not fix it; this is the phase where it becomes fixable, because
generation now writes into RDS.

- Point it at the new API and Cognito. `adminUserPassword` is enabled on the app client
  for exactly this (`infra/lib/auth-stack.ts`) — it has no browser to run SRP in.
- **Keep rule 2**: it drives the real pipeline. That is the entire reason the script exists
  rather than a fixture file, and it is now achievable again.
- **Keep rule 3**: the review history is replayed through `applyGrade`, not invented. That
  half is backend-agnostic and is the hard part; do not rewrite it.
- Restore P9's dropped acceptance criterion 2 as this phase's: `demo:seed` builds the demo
  account against the new API and its decks are visible when signed in as that account.

### 12. The RDS checkpoint — the one deploy this phase makes

**Do this when the pipeline needs a database, and not before.** The trigger is task 4's
acceptance path: the moment accepting drafts must write real `cards` rows that the app
then reads, local Postgres stops being sufficient because the Lambdas doing the writing
live in AWS.

Until then every task above runs against `localhost` and costs nothing.

When the trigger arrives, this is one focused session:

- `npm run infra:diff` and read every line. **`ALERT_EMAIL` must be set** — without it the
  foundation stack's four budgets and the alert subscription are destroyed, which
  `cdk diff` will show as `[-]` entries. That is the P8 cost governance disappearing at
  the exact moment spending starts.
- `npm run infra:deploy`. Expect the $10 and $15 budget alarms to fire. That is correct.
- Invoke the migration Lambda with `{"statusOnly": true}` **first**, then for real. This is
  the first run against **PG 17**; local has been 18.
- Repoint `.env.local`: `VITE_API_URL` to the API Gateway endpoint, and `dev-api.mjs`
  becomes a local convenience rather than the backend.

**Close the four things P9 could not**, and record each in
[P9-aws-slice.md](P9-aws-slice.md) rather than here:

1. **Criterion 9 — cold start in a VPC.** Measure it. It decides whether interactive paths
   need provisioned concurrency, and P9 task 2 has a paragraph waiting for the number.
2. **The rest of criterion 3** — the same loop through API Gateway rather than
   `dev-api.mjs`.
3. **Re-run the cross-tenant check against RDS.** P9's evidence is against local Postgres.
   The SQL is identical, so this is confirmation rather than discovery — but it is the
   claim the whole tenancy story rests on, and it should be true of the thing actually
   serving users.
4. **Criterion 10** — `aws ec2 describe-nat-gateways` returns empty *after* a VPC exists.
   It has only ever been checked against an account with no VPC in it, which is a weaker
   statement than it looks.

**Then stop the instance between sessions.** RDS can be stopped for up to 7 days; compute
stops billing and storage (~$2.30/mo) continues. It is the difference between ~$14/mo and
~$4/mo for a database nobody is querying overnight.

### 13. Record what Phase C inherits, and write P11

- **The exam work on this branch is Phase C and still runs on fixtures.** `src/features/exam/`,
  `src/lib/exam.ts`, `src/lib/mastery.ts`, `src/features/progress/MasteryMap.tsx`. It
  imports neither `supabase` nor `@/lib/queries`, which is why P9 and P10 both rewrote the
  data layer underneath it without touching it. **Phase C wires it to real data** — that is
  a substantial task and it should be planned, not discovered.
- P11 must settle §7 questions 8 (what grading means per question kind), 9 (whether an exam
  attempt resumes), and 10 (which signal wins when FSRS and the exam disagree) — question
  10 being, per the brief, "the interesting part".
- Update the board in [README.md](README.md).

---

## Acceptance criteria

1. A PDF dropped on `/create/document` produces cards, through S3 → Step Functions →
   Bedrock → DynamoDB → the review gate.
2. A scanned PDF is refused **in the browser**, before the upload, with copy that says why.

   **🔴 Not met.** `/create/document` says up front that the text must be selectable and
   that a scan cannot be read (task 3), which is the copy half. The *detection* half is
   deferred: it needs a PDF parser on the client, and the owner chose not to take that
   dependency in the session that built the upload path. Until it lands, a scan uploads
   successfully and fails later in the pipeline — exactly the outcome this criterion
   exists to prevent.
3. A job with failed chunks shows what failed and still offers the cards that succeeded.
4. `/create/text` runs through the same state machine. Nothing calls the Edge Function.
5. `npm run demo:seed` builds the demo account against the new API and its decks are
   visible when signed in as that account.
6. Topics extracted from two documents on the same subject **reconcile into one set**, not
   two. Checked by hand with two real documents, and the output recorded here.
7. **The browser drives the app end to end** (task 1) — sign in, create a deck, practise —
   against `dev-api.mjs`. P9 proved the handlers with a script; this proves `queries.ts`
   and `api-client.ts`, which no browser has yet exercised.

   **✅ Green, 2026-09-06 (session 2).** First proven at the network layer — all 18 routes
   driven through `queries.ts` and `api-client.ts` with a real Cognito token, which found
   and fixed the timestamp-format bug (`c854e8e`) — and then closed properly by the owner
   driving the rendered app against `dev-api.mjs` at `localhost:5173`. React Query's
   caching and the forms above the fetch are now exercised, which is what the criterion
   asked for and what Node could not provide.
8. **P9's four open items are closed at the RDS checkpoint** (task 12) and recorded in
   `P9-aws-slice.md`, not here: cold start measured, criterion 3 completed through API
   Gateway, the cross-tenant check re-run against RDS, and `describe-nat-gateways` empty
   *with a VPC deployed*.
9. `npm run verify` is green, including the data-access lint extended to DynamoDB.
10. **`dev-api.mjs` and `api-stack.ts` expose the same routes.** Checked, ideally
    mechanically (task 1b).

    **✅ Done 2026-09-06** (`cb9cb4f`). `scripts/check-routes.mjs` runs in `verify` and
    compares both route tables; 18 routes, identical. Drift in either direction fails the
    build, and so does the check parsing nothing.

11. `/progress` still says it is showing pre-migration data — it is still true after this
    phase, and the sentence is only removed in Phase F.
12. **The Bedrock spend for this phase is a measured number, not an estimate**, from task
    10's per-job accounting — and it is written into the brief's §6, whose current RDS
    line is already known to be wrong (it claims free tier; this account is on the newer
    credit-based plan).

---

## Decisions to record

Into `SPEC.md`, in the commit that implements them:

- What a draft is, once drafts live in DynamoDB before acceptance (task 4).
- The quota unit for document work (task 8) — a product decision, and §4.1 step 3 currently
  describes the old one.
- Where topics come from and how they reconcile (task 7), plus an ADR.

Into `docs/plans/P9-aws-slice.md`:

- The cold-start number, replacing task 2's placeholder.
- The cross-tenant check output, under acceptance criterion 5.
- `generations` moving off Supabase, in the split table.

---

## What went unverified

Fill this in as the phase runs; these are the ones already predictable:

- **The client half of P9 — now partly closed, and honestly a smaller gap than expected.**
  Task 1 drove all 18 routes through `queries.ts` and `api-client.ts` with a real Cognito
  token, so the network contract is evidence rather than transcription. The row-shape
  mismatch this bullet predicted **does not exist** — checked mechanically against the RDS
  schema. What is still unproven is the layer above the fetch: React Query cache keys, the
  optimistic updates in `useReviewCard`, and the forms. Those need a rendered page, and
  nothing here has rendered one.
- **Everything that only appears at the RDS checkpoint** (task 12): API Gateway's routing
  and authorizer, PG 17 rather than 18, and cold start. Deferring them is a cost decision,
  not a claim that they work.
- **`dev-api.mjs` drifting from `api-stack.ts`.** The one failure mode this development
  setup introduces that production does not have. Task 1b is the mitigation and it is a
  convention until someone makes it a check.
- **Cross-tenant isolation on DynamoDB.** `userId` as a partition key is structurally
  stronger than a `where` clause, but nothing proves the key is built correctly either.
- **Topic reconciliation.** Name matching will be wrong at the edges by construction, and
  the phase accepts that rather than pulling pgvector forward. Criterion 6 checks it once,
  by hand, with two documents.
- **Cost.** The $0.45-per-loop figure is the brief's estimate. Task 10's accounting is what
  turns it into a measurement, and the first month's bill is what checks that.
