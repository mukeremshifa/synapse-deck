# FR drift log

**One line per thing a phase learned that changes a later phase.**

FR1–FR7 were planned on 2026-09-07, before FR0 executed. That was a deliberate choice
against [the one-at-a-time convention](README.md), taken because the plans are written at
**contract-and-handoff altitude** — scope, preconditions, acceptance criteria and explicit
handoffs, with no file-by-file task lists, which is the part that rots.

The convention's objection still stands and this file is the answer to it:

> A plan written today against code that does not exist is fiction, and every drift between
> plan and reality is a session confidently doing the wrong thing.

**So every drift gets written down, by the session that discovers it, at the moment it is
discovered.** Not at the end, and not "if it seems significant" — the whole point is that
the session which knows is the one that records it.

---

## The duty

Every FR plan carries two mandatory steps that exist only to feed this file:

- **§1b Reconcile** — the *first* thing a session does, before writing any code. Read the
  entries below that name your phase. Read the previous phase's handoff section. Where
  reality differs from your plan, **fix your plan and commit that first.**
- **§N Update what follows** — the *last* thing. Anything you built that invalidates an
  assumption in a later plan goes here, **and** into that plan directly.

Updating a later plan is not scope creep and is not optional. It is the mechanism that
makes planning seven phases ahead defensible rather than reckless.

## How to write an entry

Name the phase that found it, the phase(s) affected, and what a session should now *do*
differently. "The contract changed" helps nobody; "FR4's generate modal must handle a
`Job` that fails before any stage reports — `fake.ts` can produce this" is an instruction.

Append; never rewrite history. A superseded entry gets a follow-up entry, not an edit.

---

## Entries

| Date | Found by | Affects | What changed, and what to do about it |
| --- | --- | --- | --- |
| 2026-09-07 | Planning | FR0 | The seam `fake.ts` was to sit behind **does not exist**. `api.get<T>(path)` is verb-shaped, so no fake can implement it in a way tsc checks. Inventing the `ApiClient` interface is FR0 task 1, not task 3. |
| 2026-09-07 | Planning | FR0 | `env.ts` throws at startup on missing AWS vars, so a fresh clone dies on boot in fake mode. FR0 makes them conditional on `VITE_API_MODE`. |
| 2026-09-07 | Planning | FR1 | The brief names `grade-tokens.ts`, `Meter`, `RatingButtons`, `DiagnosticPage` as ramp consumers. **Only `SessionSummary.tsx` imports the module**; the others reach the ramp through Tailwind classes (`bg-grade-again`) defined in `globals.css`. Re-deriving the palette must cover the CSS custom properties, not just the TS. `DiagnosticPage.tsx` is at `src/features/plan/`, not `src/features/mastery/`. |
| 2026-09-08 | FR0 | FR6, FR2 | **`/progress` does not exist.** It was removed before FR0, so three of the four Supabase stats hooks had no consumer and the fourth (`useReviewHistory`) served one number on `DashboardPage`. FR0's task-5 options both assumed the route. All four hooks were **deleted**, not re-pointed; the aggregates now live on `ApiClient` as `getReviewHistory` / `getDueForecast` / `getCardStates` / `getRetention`, **notebook-scoped**. FR6 builds the overview from those four and must not look for `queries.ts` hooks. |
| 2026-09-08 | FR0 | FR2 | The dashboard's **streak card was removed**, not left loading: `streaks()` needs day-bucketed history that only the Supabase RPC had, and `value={null}` renders a permanent skeleton. FR2's home screen gets the streak from `getGlobalSummary().streakDays`, which exists in the contract and works in fake mode. In `live` mode it returns 0 — the AWS `/summary` route has no streak, and adding one is FR7's. |
| 2026-09-08 | FR0 | FR2–FR6 | **The contract is paginated.** `listNotebooks`, `listSources`, `listArtifacts`, `listCards` and `listAttempts` return `Page<T>` (`{ items, nextCursor }`), not arrays. The brief did not settle this; FR0 decided it, because a list that would page at FR7 must page now or every screen is designed having never seen a second page. Every consuming hook reads `.items`. |
| 2026-09-08 | FR0 | FR2, FR5 | **`getGlobalSummary` is the one cross-notebook method**, for the home strip (§3.5). Its rule, in the contract's doc comment: **nothing on it may become a CTA** — a button there would have to pick a notebook, which is the `focus` guess the re-architecture deletes. Every action lives on a notebook card. |
| 2026-09-08 | FR0 | FR4 | **`fake.configure({ failNextJob: { at: 'immediately', code } })` is the case to design for first.** A job can fail before any stage reports, with `unitsTotal` still 0 — a quota refusal does exactly this. That is *not* the same as "no progress yet", and a progress surface that renders them identically is wrong. Also available: `latencyMs`, `failNext`, `failAlways`, `jobDurationMs`, `truncateNextJob`. |
| 2026-09-08 | FR0 | FR3, FR5, FR6 | **A dangling `sourceId` is live in the fixtures.** `art-deck-abx` names `src-pharm-deleted`, which `listSources` does not return, and a note block in `art-notes-resistance` cites it too. **Render provenance from `sourcesSnapshot`** (complete, never dangles); use `sourceIds` only to *link*. Code that resolves ids against `listSources` and assumes a hit will throw. |
| 2026-09-08 | FR0 | FR7 | **`client.ts` throws `not_implemented` for everything the live backend lacks** — sources, artifacts, questions, attempts, note sets, and all four aggregates. FR0 §6.3 tabulates them and that table is FR7's build list. FR7 also deletes the synthetic-artifact translation in `client.ts`, where a notebook's own id doubles as its implicit deck's id. |
| 2026-09-08 | FR0 | all | **Auth is lazy now.** `cognito.ts` builds its user pool on first use, not at module load, because the Cognito variables are absent in fake mode. A missing variable therefore fails at first sign-in rather than at boot. Nothing in FR1–FR6 should need to sign in; if a phase does, it needs `VITE_API_MODE=live` and a real `.env.local`. |

| 2026-09-08 | FR1 | FR2–FR6 | **The grade ramp is two ramps.** A stop light enough for ink (≥4.5:1) is too light to be a mark on paper (≥3:1), so `--grade-*` is the **field** ramp (a background, ink on top) and `--grade-*-mark` is the **mark** ramp (a dot, chart stroke, icon, rule). In TS: `GRADE_FIELD_TOKEN` / `GRADE_MARK_TOKEN` — `GRADE_TOKEN` and `STATE_TOKEN` no longer exist. Setting `backgroundColor` with text over it → field; setting `fill`/`color` → mark. Guessing wrong typechecks fine and renders a 1.21:1 invisible dot, which is the bug this split fixes. |
| 2026-09-08 | FR1 | FR2–FR6 | **`--border` is decorative; `--border-strong` is for meaning.** A hairline dark enough to pass SC 1.4.11 does not read as a hairline. Field outlines, selected states and focused controls use `--border-strong` / `--input` (3.17:1); card hairlines and separators use `--border` (1.35:1). `scripts/check-contrast.mjs` checks the former and deliberately not the latter. |
| 2026-09-08 | FR1 | FR2–FR6 | **`EmptyState` moved to `src/components/states.tsx`**, beside `LoadingState`, `LoadingCard`, `ErrorState` and `GeneratingState`. `src/components/EmptyState.tsx` is now a one-line re-export so the nine existing callers still work — **import from `@/components/states` in new code**, and delete the old module when its last caller goes. |
| 2026-09-08 | FR1 | FR4 | **`GeneratingState` exists and takes `value={null}`.** That is the case FR0's drift log said to design for first: a job that fails before any stage reports, `unitsTotal` still 0. Null draws a breathing indeterminate bar; 0 draws an empty determinate one and falsely claims the size is known. `Progress` takes `number \| null` for the same reason. Do not render generation with `LoadingState` — the skeleton promises the answer already exists and is in transit. |
| 2026-09-08 | FR1 | FR2 | **The layout vocabulary is `Page`, `PageHeader`, `Section`, `SectionHeader`, `Toolbar`/`ToolbarSpacer`, `Rail`, `PaneGroup`/`Pane`/`PaneHandle`** in `src/components/layout.tsx`. `Page width="full"` drops padding for the shell. **`Rail` does not resize and `Pane` does** — that is the whole distinction. `PageHeader` owns the serif face, which is how the typography rule is enforced rather than merely documented. |
| 2026-09-08 | FR1 | FR2 | **`resizable.tsx` is written against `react-resizable-panels` v4, not v2.** The installed API is `Group` / `Panel` / `Separator` with `orientation`; the published shadcn source uses `PanelGroup` / `PanelResizeHandle` with `direction` and **will not compile** if pasted in. Our exports keep the shadcn names. Do not "fix" the file by copying upstream. |
| 2026-09-08 | FR1 | FR2 | **`command.tsx` is not `cmdk`.** It is built on our `Dialog` plus a controlled list, to hold the two-dependency line the brief drew. Exported names are `cmdk`'s (`CommandDialog`, `CommandInput`, `CommandList`, `CommandItem`, …), so adopting the package later is small. Matching is the caller's job — the component filters nothing on its own. |
| 2026-09-08 | FR1 | FR2–FR6 | **Overlay motion is `ui-overlay` / `ui-panel` / `ui-pop` / `ui-sheet` in `globals.css`, keyed off Radix's `data-state`.** There is no `tailwindcss-animate` — `animate-in`, `fade-in-0`, `zoom-in-95` and friends **do not exist in this repo** and will silently do nothing. Use the `ui-*` classes, or the `--duration-*` / `--ease-*` tokens directly. |
| 2026-09-08 | FR1 | FR2–FR6 | **Spacing is tokenised and named by role**: `hairline` 4, `tight` 8, `snug` 12, `base` 16, `gutter` 24, `section` 40, `page` 64 — as `gap-gutter`, `p-snug`, `mt-tight`. Prefer these over raw Tailwind numbers so a redesign is one file. Elevation is `shadow-raised` / `shadow-overlay` / `shadow-modal`; **`--primary` is still a field and never a foreground** (1.21:1 on paper). |
| 2026-09-08 | FR1 | all | **`node scripts/check-contrast.mjs` is the contrast gate, and it is not in `verify`.** It parses `globals.css`, so it cannot drift from the palette. **When you introduce a new token pair a screen actually draws, add it to `PAIRS` in that script** — 52 pairs pass today, and a pair nobody listed is a pair nobody checked. It found three real failures in the P5 palette that had shipped unmeasured. |

| 2026-09-08 | FR2 | FR3, FR5, FR6 | **Nothing consumed the FR0 contract until FR2, and most screens still do not.** `grep -rln "from '@/lib/api'" src` returned nothing at the start of this phase: every screen ran on `src/lib/queries.ts`, the old deck-shaped stack. FR2 built **home only** on the contract (`src/features/home/queries.ts`), so the app now runs on **two data stacks at once**, and the route table is the seam. **Your phase's first task is re-pointing your screens at `@/lib/api`** — FR3 the notebook, FR5 the runners, FR6 the overview. `queries.ts` and `api-client.ts` die with the last of them, and `src/lib/notebooks.ts`'s `toNotebook`/`isResumable` go at the same time. Do not extend `queries.ts`. |
| 2026-09-08 | FR2 | FR3, FR4 | **The modal system is `src/app/modals.tsx`, and modals are in the URL** — `?modal=<name>`, via `ModalProvider` + `useModal()`. **Add your modal's name to the `ModalName` union**; a name not in it is a compile error rather than a modal that never opens. `modalProps('name')` spreads straight onto a Radix `Dialog`/`Sheet`. One modal at a time — `openModal` replaces rather than stacks. **A confirmation is the exception and stays local state** (`ConfirmDialog`): you can be in "generating a quiz", not in "about to confirm a delete". Verified in a browser: open writes the param, a pasted URL restores the modal, Escape and the back gesture both close it and clean the URL, and an unknown name reads as closed. |
| 2026-09-08 | FR2 | FR3, FR5, FR6 | **`notebookPath` in `src/lib/notebooks.ts` was re-pointed, and every runner helper now requires an artifact id** — `practice(notebookId, deckId)`, `quiz(id, quizId)`, `exam(id, examId)`, `notes(id, noteSetId)`. `list()`, `gate()`, `cards()`, `blueprint()` and `diagnostic()` are **gone** with the routes they named; `home()` and `overview()` are new. A caller with no artifact id to pass is a surface that was about to guess, and it now fails to compile instead — that is how FR2 found the guesses in `StudioRail`, `DiagnosticPage` and `BlueprintPage`. |
| 2026-09-08 | FR2 | FR3 | **P11's notebook shell was deleted, not left for you to unpick.** `NotebookPage`, `NotebookHeader`, `SourcesRail`, `StudioRail`, `WorkspacePane`, `useAsk`, `NotebookCardsPage`, `NotebookLayout` and `use-rails` are gone (brief §1.4 lists them as not surviving). `/notebooks/:id` renders a placeholder naming FR3. **Build the three panes on FR1's `PaneGroup`/`Pane`/`Rail`**, not on P11's rails. `useAsk` and `WorkspacePane` are recoverable from git history if the chat pane wants them. |
| 2026-09-08 | FR2 | FR4 | **The `/create/*` page family is deleted**, including `CreateFromTextPage` and `CreateFromDocumentPage`. `ReviewGatePage`, `JobProgressPanel`, `PipelineStages`, `StagingList`, `useJobProgress` and `useUploadDocument` **survive on disk but are unrouted** — they are yours to mine or replace. The review gate becomes a modal over the notebook (brief §3.2). |
| 2026-09-08 | FR2 | FR5, FR6 | **`DiagnosticPage` and `BlueprintPage` are unrouted and had their artifact-less navigation removed.** The diagnostic's "drill" and "mini-exam" plan actions used to navigate to *the* practice and *the* exam of a notebook with nothing named; they now raise a toast saying what they need. The blueprint's "Sit the sample exam" button is gone for the same reason. **FR6 rebuilds both against the contract's artifact list**, which is what can finally answer "which deck drills this topic?". |
| 2026-09-08 | FR2 | FR5 | **`ExamPage` is routed at `/notebooks/:id/exams/:examId` but ignores `:examId`** — it still renders the hardcoded `SAMPLE_EXAM` from `src/features/exam/fixtures.ts` on the old stack. The route is honest; the page is not yet. Same for `PracticePage`, which reads `:notebookId` and passes it to the old stack as a deck id. **Both 500 against a local dev-API when given fake-fixture ids**, which is the two-stack seam rather than a bug in either. |
| 2026-09-08 | FR2 | all | **A browser can be driven from a session over CDP, with no new dependency.** FR1 shipped its one visual criterion unmet for want of browser tooling. Chrome is installed on this machine and Node 24 has a built-in `WebSocket`, so ~60 lines speaks the DevTools Protocol: navigate, evaluate, type, click, screenshot, and collect console errors. **This is how FR2 met criteria 8 and 9** and how it found four defects nothing else could see. Sign in with `DEMO_EMAIL`/`DEMO_PASSWORD` from `.env.local`; run Vite on **port 5173** or the dev-API's CORS allowlist rejects every old-stack call. |
| 2026-09-08 | FR2 | FR3–FR6 | **Auth is real even in fake mode.** `VITE_API_MODE` selects the *data* implementation only; `AuthProvider` always talks to Cognito, and `.env.local` has a working pool. So a phase that needs to see a protected screen signs in with the demo account — FR0's drift row ("nothing in FR1–FR6 should need to sign in") is **wrong in practice**: every route but `/login`, `/signup` and the 404 is behind `ProtectedRoute`. |

| 2026-09-08 | FR3 | FR4 | **The generate modal's entry point exists and is `?modal=generate&kind=<ArtifactKind>`.** Studio's four artifact entries open it — an empty entry from its one button, a non-empty one from "+ New …" under the list. `GenerateModalPlaceholder.tsx` is the seam: **delete that file and put the real modal at the same param contract.** `kind` is validated with `ArtifactKind.safeParse` rather than trusted, because a modal's params are user-editable text; keep that. What it must collect is `CreateArtifactInput` for the kind, and what it returns is a `Job`. |
| 2026-09-08 | FR3 | FR4 | **The pending state FR4 replaces is a poll, not a progress surface.** `useSources` and `useArtifacts` (`src/features/notebook/queries.ts`) set `refetchInterval: 1500` while any row is `processing` / `generating`, and the row shows the word "Processing…". That is the honest minimum, not a design. FR4 replaces it with `getJob`, and FR0's drift row still names the case to build first: a job that fails before any stage reports, `unitsTotal` still 0. **Both polls should be removed when the job surface lands** — leaving them means two mechanisms watching the same thing. |
| 2026-09-08 | FR3 | FR4 | **Save-as-note landed in FR3, not FR4.** `useSaveResponseAsNote` calls `createArtifact({ kind: 'noteset', fromResponseId })`, so a chat answer becomes a real note-set artifact that appears in Studio under Notes. It is a generation like any other and **returns a `Job`**, which means it is a second caller of whatever progress surface FR4 builds — not just the generate modal. The title is the user's question, trimmed to the contract's 200 characters. |
| 2026-09-08 | FR3 | FR5, FR6 | **`src/lib/notebooks.ts` is now route construction and nothing else.** `toNotebook`, `isResumable` and that file's local `Notebook` type are **deleted** — FR3's shell was their last consumer, and FR2's comment said to delete each as its caller went. So the file no longer dies with `queries.ts`: `notebookPath` outlives it and is still where every URL is built. Import `Notebook` from `@/lib/api`. |
| 2026-09-08 | FR3 | FR5, FR6 | **Two of the three remaining old-stack screens are yours.** After FR3, `@/lib/api` serves home and the notebook; `queries.ts` still serves settings, the practice runner, the exam runner, `BlueprintPage`, `DiagnosticPage` and `ReviewGatePage`. `useProfile` is the one hook with a consumer outside your phases (`AccountMenu`, `SettingsPage`) — **it, and not the whole file, is what will be left when FR5 and FR6 finish.** Plan to re-point it rather than assuming `queries.ts` disappears on its own. |
| 2026-09-08 | FR3 | FR4–FR6 | **`PaneGroup`'s `autoSaveId` did nothing until FR3 and now works.** FR1 documented the prop and passed it through untyped; it is a **v2** prop and the installed library is **v4**, which replaces it with the `useDefaultLayout` hook. `src/components/layout.tsx` now implements the documented name over that hook, with `onlySaveAfterUserInteractions` so a window resize cannot overwrite a drag. Any `PaneGroup` you add gets persistence by passing `autoSaveId`; it must be unique per group, or two shells restore each other's layouts. |
| 2026-09-08 | FR3 | FR4–FR6 | **Do not switch layouts with `hidden md:flex` on a `PaneGroup`, and be careful doing it anywhere.** Two distinct traps, both found in a browser rather than by reading code. (1) `ResizablePanelGroup` hardcodes `flex` in its own `cn(...)`, and tailwind-merge treats a caller's `hidden` as the same conflict group — the class is silently dropped and **both layouts render at once**. (2) Even with a wrapper div fixing that, CSS hiding still **mounts** both branches: every pane exists twice, every query gets two subscribers, and the accessibility tree lists everything twice. FR3 added **`useMediaQuery` + `MEDIA_WIDE` to `src/components/layout.tsx`** (a `useSyncExternalStore`, so the first paint is right) and branches in JS. Use it when the two layouts must not both exist; a responsive class is still correct when they may. |
| 2026-09-08 | FR3 | FR5, FR6 | **Render untrusted text with `AnswerText`, or with a renderer built the same way.** `src/features/notebook/AnswerText.tsx` turns a chat answer into elements — bold, inline code, `[n]` citation markers, bullets, paragraphs — and **cannot emit HTML**, because it never builds a string anything parses as markup. There is deliberately **no markdown dependency**: FR1's two-dependency line holds, and the safety is structural rather than a `skipHtml` option someone can get wrong later. It understands those forms and nothing else. **Do not grow it into a markdown engine** — a surface that needs real structure should take `NoteBlock`s, which the contract already provides as a discriminated union for exactly this reason. |
| 2026-09-08 | FR3 | FR5, FR6 | **Provenance renders from `sourcesSnapshot`, and the dangling case is live on the first screen.** `Provenance` in `StudioPane.tsx` is the worked example: names come from the snapshot (complete, never dangles), and `sourceIds` only decides whether a name is *live* — a deleted source renders **struck through with a warning icon, never omitted**, because an artifact that silently drops a source is lying about what it was built from. Verified in a browser by deleting a real source: eight artifacts survived with their provenance intact. Reuse the shape; do not resolve ids against `listSources` and assume a hit. |
| 2026-09-08 | FR3 | FR4 | **`Checkbox` now exists** at `src/components/ui/checkbox.tsx`, from `radix-ui` — no new dependency, since that package was already the one FR1's line allows. Borders use `--input` rather than `--border`, per FR1's decorative/meaningful split: an unchecked checkbox is nothing but its outline, and the decorative token draws a 1.35:1 suggestion of a control. |

| 2026-09-08 | FR4 | FR5, FR6, FR7 | **There is no review gate in the contract, and FR4 did not invent one.** `Card.status` is `active \| suspended`; there is no `deck_status`, no draft, and nothing in `ApiClient` accepts or rejects a generated card. FR0 dropped the concept deliberately, so FR4's task 4 had nothing to build against. What shipped is the part the contract *can* express: a **completion surface** in the Studio that reports partial success from `Job.truncated` / `unitsFailed` / `unitsCompleted`, with one exit ("Open it") — which is the property task 4 actually wanted, since the old page's bug was navigating to a route that did not exist. `useDraftCards`, `useAcceptDrafts` and `useFinishReviewGate` were **deleted** from `queries.ts` with their last callers. **Card-by-card triage needs a contract change**; do not add it to a runner without deciding that first. |
| 2026-09-08 | FR4 | FR5, FR6, FR7 | **A running job cannot name the artifact it is building.** `Job.result` is documented as populated *only on `succeeded`*, so while a job runs — and when one **fails** — `result.artifactId` is null. Two consequences you inherit: a progress bar *inside* an artifact row is not expressible (FR4 renders a panel above the list instead), and a failed job can only be matched to its `failed` artifact heuristically. **Render a failure from the job** (it carries `error.code`) and the leftover row from the artifact; doing it the other way round loses the reason, which FR4 found in a browser — every injected `quota_exceeded` read as "stopped without reporting a reason". FR7 could add a running `result.artifactId` if a per-row bar is wanted. |
| 2026-09-08 | FR4 | FR5, FR6 | **In-flight work is `useNotebookJobs(notebookId)` in `src/features/notebook/jobs.ts`**, and it is the *only* thing that should watch a job. It polls `listJobs` at 1.5s while anything is running, stops dead when nothing is, and **invalidates `detail` / `sources` / `artifacts` / home's grid once per completion** — not per poll, and deliberately not `notebookKeys.all`, which is a prefix of the jobs key and would refetch the observer. FR3's two `refetchInterval` polls on `useSources` and `useArtifacts` are **gone**. If a surface of yours needs to know something finished, subscribe to this rather than adding a third mechanism. |
| 2026-09-08 | FR4 | FR5, FR6, FR7 | **§6.3 decided: a regeneration is a NEW artifact, never a replacement.** Nothing in FR4 mutates or reuses an existing artifact's id, and "Try again" on a failure reopens the generate modal rather than resubmitting. So **a runner may treat an artifact's id, contents and `sourcesSnapshot` as stable for its lifetime** — an attempt recorded against an artifact id can never be invalidated by a regeneration, because a regeneration produces a different id. This matches brief §1.2(7) and is the simpler of the two options §6.2 left open. The cost, recorded: regenerating leaves the old artifact in the list, and deleting it is the user's explicit act. |
| 2026-09-08 | FR4 | FR5, FR6 | **`closeModal` now removes the params the modal opened with.** `openModal('generate', { kind })` records the keys it set in a `modalParams` search param, and closing deletes them. Before this, closing left `?kind=quiz` on the notebook for ever — a URL the user could share or reload carrying a parameter that meant nothing. Found in a browser. **Any modal you open with params gets this for free**; a hand-typed URL that omits `modalParams` still leaks, which is the honest limit of not knowing what a param was for. |
| 2026-09-08 | FR4 | FR7 | **The fake now resolves what a failed job was building — and the live backend must too.** `createArtifact` pushes the artifact at `generating` *before* the job runs, so a failed job used to leave that row spinning for ever. `startJob` gained an `abandon` hook (the mirror of `commit`): on failure the artifact flips to `status: 'failed'` and **no contents are written**, and a failed `add-source` marks its source `failed` with the reason. Verified in a browser: a failed deck generation left an artifact with **0 cards**. FR7's pipeline needs the same guarantee — a job that dies must not leave a permanently `generating` row. |
| 2026-09-08 | FR4 | FR5, FR6 | **Generation failure copy is a closed table**: `failureFor(code)` in `src/features/notebook/generation-errors.ts` is a `Record<ApiErrorCode, { title, detail, retry }>`, so a new code in the contract fails to compile until someone decides what it says. **`retry` is the field that matters** — it is what decides whether a "Try again" button appears, and offering a retry that cannot work (`quota_exceeded`, `refused`) is worse than offering none. Reuse this rather than rendering `error.message`, which has the server writing the UI's copy. |

_(Append below. Do not delete rows.)_
